---
title: "Hook Events: The Five You Need to Know"
slug: "hook-events"
order: 1
difficulty: "advanced"
duration: 18
tags: ["hooks", "events", "automation", "configuration"]
objectives:
  - "Understand the five canonical hook events and when each fires"
  - "Know the difference between command and HTTP hook types"
  - "Use exit codes to control Claude's behavior from hooks"
  - "Know that Claude can write hooks for you"
quiz:
  - id: "hooks-1"
    question: "What does exit code 2 do in a PreToolUse hook?"
    type: "multiple-choice"
    options:
      - "Continues normally"
      - "Blocks the tool use — Claude cannot proceed with that action"
      - "Retries the tool use"
      - "Exits Claude Code"
    correct: 1
    explanation: "In a PreToolUse hook, exit code 2 blocks the tool use entirely. Claude receives a message that the action was blocked by a hook and must find an alternative approach. Exit 0 allows the tool use to proceed. This is the mechanism for creating safety gates."
  - id: "hooks-2"
    question: "Which five hook events does Claude Code officially expose?"
    type: "multiple-choice"
    options:
      - "PreTool, PostTool, Start, Stop, Alert"
      - "PreToolUse, PostToolUse, SessionStart, SessionEnd, Notification"
      - "Read, Write, Execute, Notify, Clean"
      - "Any event you define in settings.json"
    correct: 1
    explanation: "The five canonical hook events are PreToolUse (before a tool runs), PostToolUse (after a tool runs), SessionStart (session begins), SessionEnd (session ends), and Notification (Claude needs your attention). These cover the full lifecycle you need for automation."
  - id: "hooks-3"
    question: "What is the difference between command and HTTP hooks?"
    type: "multiple-choice"
    options:
      - "Command hooks are faster"
      - "Command hooks run a shell command locally; HTTP hooks POST to a URL"
      - "HTTP hooks are more secure"
      - "They're the same"
    correct: 1
    explanation: "Command hooks execute a shell command on your machine (like running a formatter or sending a desktop notification). HTTP hooks send a webhook to a URL (like posting to Slack or triggering a CI pipeline). Use command for local actions, HTTP for remote notifications."
  - id: "hooks-4"
    question: "Why are hooks considered deterministic while CLAUDE.md is advisory?"
    type: "multiple-choice"
    options:
      - "Hooks use more tokens than CLAUDE.md"
      - "Hooks always execute via the harness — Claude cannot skip or reinterpret them. CLAUDE.md instructions are guidance the model may or may not follow perfectly."
      - "CLAUDE.md is for humans"
      - "There is no real difference"
    correct: 1
    explanation: "Hooks are executed by the Claude Code harness, not by the model — when a PreToolUse hook exits 2, the action is blocked with zero chance of the model talking itself around it. CLAUDE.md is advisory: the model reads it and usually follows it, but nothing forces compliance. For hard rules you cannot afford to miss, use hooks."
---

## The Hook System

Hooks are automated actions that fire in response to Claude Code events. Every time Claude reads a file, writes code, runs a command, or needs your attention, a hook event fires. You can attach your own scripts to these events — turning Claude Code into an automated pipeline.

Think of hooks as event listeners. The event fires, your hook runs, and depending on the exit code, Claude's behavior may change.

## Hook Types

### Command Hooks

Run a shell command on your machine:

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write(*.ts)",
        "command": "npx prettier --write $file"
      }
    ]
  }
}
```

The command runs in your shell with access to environment variables and the file system.

### HTTP Hooks

Send an HTTP request to a URL:

```json
{
  "hooks": {
    "Notification": [
      {
        "type": "http",
        "url": "https://hooks.slack.com/services/xxx/yyy/zzz",
        "method": "POST",
        "body": {
          "text": "Claude Code needs your attention"
        }
      }
    ]
  }
}
```

HTTP hooks are ideal for remote notifications — Slack, Discord, webhooks, CI triggers.

## The Five Hook Events

Claude Code exposes five hook events. Every automation you build maps to one of these.

### 1. PreToolUse

**Fires**: Before Claude executes a tool (Read, Write, Bash, MCP tool).

**Use case**: Safety gates. Block dangerous operations before they happen.

**Exit codes**:
- `0` — Allow the tool use
- `2` — Block the tool use (Claude sees an error message and must try a different approach)

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write(src/generated/**)",
        "command": "echo 'Cannot write to generated files' && exit 2"
      }
    ]
  }
}
```

### 2. PostToolUse

**Fires**: After Claude successfully executes a tool.

**Use case**: Auto-formatting, linting, type-checking, running tests.

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write(*.ts)",
        "command": "npx prettier --write $file"
      },
      {
        "matcher": "Write(*.py)",
        "command": "python -m black $file"
      }
    ]
  }
}
```

The `$file` variable contains the path of the affected file.

### 3. SessionStart

**Fires**: When a new Claude Code session begins.

**Use case**: Environment setup, service health checks, status banners.

```json
{
  "hooks": {
    "SessionStart": [
      {
        "command": "echo 'Checking services...' && docker compose ps"
      }
    ]
  }
}
```

### 4. SessionEnd

**Fires**: When a Claude Code session ends (you quit, or it exits cleanly).

**Use case**: Cleanup, log rotation, summary generation, saving transcripts.

```json
{
  "hooks": {
    "SessionEnd": [
      {
        "command": "echo \"$(date): session ended\" >> ~/.claude/session-history.log"
      }
    ]
  }
}
```

### 5. Notification

**Fires**: When Claude needs your attention — waiting for permission approval, idle after finishing work, or hit an error it can't resolve.

**Use case**: Desktop notifications, sound alerts, Slack messages, mobile pings.

```json
{
  "hooks": {
    "Notification": [
      {
        "command": "osascript -e 'display notification \"Claude needs you\" with title \"Claude Code\"'"
      }
    ]
  }
}
```

## Let Claude Write Hooks For You

You don't have to write hook JSON by hand. Ask Claude:

```
"Write a PostToolUse hook that runs eslint --fix after every
TypeScript file edit."
```

Claude will produce the matcher, the command, and valid JSON, and can drop it straight into `.claude/settings.json`. This is the fastest way to grow your hook library — describe the automation, let Claude translate it to configuration.

## Matchers

Hooks use **matchers** to filter which tool uses trigger them. Without a matcher, a hook fires for all tool uses of that event type.

```json
{
  "PostToolUse": [
    {
      "matcher": "Write(*.ts)",
      "command": "npx prettier --write $file"
    },
    {
      "matcher": "Write(*.css)",
      "command": "npx prettier --write $file"
    },
    {
      "matcher": "Bash(pnpm test*)",
      "command": "echo 'Tests ran at $(date)' >> test-log.txt"
    }
  ]
}
```

Matchers use the same glob pattern syntax as permissions:
- `Write(*.ts)` — Any TypeScript file write
- `Write(src/**)` — Any file write under src/
- `Bash(pnpm test*)` — Any pnpm test command
- `Read(.env*)` — Any .env file read

## Hook Variables

Hooks receive context about the event through environment variables:

| Variable | Description | Available In |
|---|---|---|
| `$file` | Path of the affected file | PostToolUse (Write) |
| `$tool` | Name of the tool used | PreToolUse, PostToolUse |

## Configuration Location

Hooks are configured in `settings.json` at two scopes:

- **Global**: `~/.claude/settings.json` — applies to every project
- **Project**: `.claude/settings.json` — committed to git, shared with the team

Project hooks require explicit approval the first time Claude Code loads them — this protects you from cloning a repo that tries to run arbitrary commands.

```json
// .claude/settings.json (project-level, shared with team)
{
  "hooks": {
    "PreToolUse": [...],
    "PostToolUse": [...],
    "Notification": [...]
  }
}
```

## Quick Reference

| Event | When It Fires | Common Use |
|---|---|---|
| PreToolUse | Before tool execution | Safety gates, blocking |
| PostToolUse | After tool execution | Formatting, linting, type-check |
| SessionStart | Session begins | Environment setup, status banner |
| SessionEnd | Session ends | Cleanup, log summary |
| Notification | Claude needs attention | Desktop alerts, Slack ping |

## Hooks vs CLAUDE.md

Both shape Claude's behavior, but they work very differently:

| | CLAUDE.md | Hooks |
|---|---|---|
| Executed by | The model (reads, tries to follow) | The harness (runs unconditionally) |
| Reliability | Advisory — usually followed, sometimes missed | Deterministic — always fires |
| Best for | Conventions, stack, style rules | Hard safety rules, formatting, alerts |

Use CLAUDE.md for "please always use named exports." Use hooks for "never let a write to `.env*` succeed."

## Key Takeaway

Claude Code has five hook events: PreToolUse, PostToolUse, SessionStart, SessionEnd, and Notification. The two most immediately useful are PostToolUse (auto-format files after Claude writes them) and Notification (get alerted when Claude needs you). Hooks can be shell commands (local actions) or HTTP requests (remote notifications). Exit code 2 in PreToolUse blocks the action entirely — that's how you build safety gates that Claude cannot talk around. And if you don't want to write the JSON yourself, just ask Claude: "write a hook that runs my formatter after every edit."
